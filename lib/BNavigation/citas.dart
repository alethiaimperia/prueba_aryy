import 'package:flutter/material.dart';

class Citas extends StatelessWidget {
  const Citas({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('AQUI IRA SECCION DE CITAS AGENDADAS'),
    );
  }
}
